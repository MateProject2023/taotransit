# План: taotransit.com — Tilda → Astro + Tailwind

Статус: **этап 1 выполнен**, этапы 2–4 ждут. Составлен 2026-08-31.

Приоритет заказчика: **сначала вёрстка и показ**. Форма, аналитика, хостинг и переезд домена — после того, как вёрстка принята.

---

## Этап 1. Вёрстка — ✅ сделано 2026-08-31 (+ проход «лоб в лоб»)

Цель: работающая статическая копия лендинга, улучшенная по адаптиву, на превью-ссылке — показать заказчику.

### 1.1 Снять эталон
- ✅ **Сделано в ходе разбора:** ассеты выгружены — 15 изображений + фавиконка, 6.6 МБ, это оригиналы (бо́льших вариантов на CDN нет). Собирать надо грепом всех `static.tildacdn.*` URL из HTML: Zero Block прячет ссылки в inline-JS, разбор `data-original`/`srcset` находит 4 из 15.
- Скриншоты каждой секции на 1440 / 1199 / 959 / 639 / 479 px через Playwright → `reference/`.
- **Это приёмочный материал:** после пересборки сверяемся с ним посекционно.

### 1.2 Вытащить контент
- Весь текст (~9 800 символов) в один модуль `src/content/landing.ts` — заказчик будет править копирайт, он не должен лежать в разметке.
- Ассеты прогнать через `astro:assets` (webp/avif, явные размеры) — PNG несжатые, 6.6 МБ ужмутся кратно.
- Фавиконка с текущего сайта.

### 1.3 Каркас
- `npm create astro@latest` (Astro 7.2), Tailwind 4.3 через `@tailwindcss/vite`.
- Токены в `@theme` (`src/styles/global.css`): фон `#11101c`, текст `#ebebf7`, акцент `#5b35e5`, светлый акцент `#977bff`.
- CraftworkSans копируем из `../mate/mate/src/styles/fonts/` (5 начертаний woff2, уже есть) — `font-display: swap` + preload.
- Базовый layout, `<head>`, контейнеры, типографическая шкала.

### 1.4 Пересборка секций
13 видимых секций (в таблице `CLAUDE.md` 16 строк — там же осиротевший попап и служебный оверлей), примерно в этом порядке:

1. Header + мобильное меню (бургер)
2. Hero + CTA-кнопка на бота
3. «За все время мы успели…» — счётчики
4. «Почему мы?» — гарантии
5. «Услуги» — карточки
6. «Тарифы по доставке» — таблица (авто/жд/авиа, сроки) ← самая сложная на мобильном
7. «Этапы работы» — нумерация V→I
8. «Наши кейсы»
9. «Tao Transit это про:» + миссия
10. «Как начать работу?» → консьерж-бот
11. «Рост и развитие»
12. Финальный CTA
13. Футер (контакты, почта, тг-канал, менеджер, бот)

Правила: семантический flex/grid вместо координат Zero Block; ноль JS кроме бургера и якорной прокрутки; кнопки-ссылки на бота остаются как есть.

**Границы «улучшаем»: только адаптив и контраст.** Композицию, порядок и визуальную идею секций не трогаем — цель «максимально похоже, но правильно сделано». Если после показа захочется поправить композицию — отдельным заходом.

### 1.5 Базовый SEO и приёмка
- `meta description` (сейчас его нет), OG-теги, JSON-LD `Organization`, свой `robots.txt`, `sitemap.xml` (у Тильды ссылка на ситемап по `http://` — не копируем).
- Lighthouse, посекционное сравнение со скриншотами, проверка контраста (текстовые ссылки — `#977bff`, не `#5b35e5`).
- Превью-деплой на площадку, **проверенную из РФ без VPN** — заказчик откроет ссылку из России, а Cloudflare Pages и Vercel там режутся. Безопасный дефолт: статика отдельной папкой на VPS `mate` или любой RU-доступный хостинг.

**Результат (2026-08-31):** собрано и проверено локально. Lighthouse на превью-сборке —
**Performance 99, Accessibility 100, Best Practices 100, SEO 100**, CLS 0, LCP 2.0 с.
Ноль внешних запросов, ноль ошибок в консоли. Вес сборки 4.7 МБ, из них 4.1 МБ — фоновое видео.

Осознанные расхождения с оригиналом:

| Что | Почему |
|---|---|
| Фоновое видео hero отдаём сами (`<video>`), а не через iframe Rutube | оригинал ради 20 секунд немого фона тянул плеер и **два чужих счётчика Метрики** |
| Ролик о боте — click-to-load фасад | контентное видео остаётся у заказчика на Rutube, но не грузится всем подряд |
| Ссылки-строки набраны цветом `#977bff` | `#5b35e5` на фоне даёт 2.8:1 — провал WCAG AA |
| Римские цифры II и IV в этапах — `#7d61f5` | в оригинале 2.76:1, ниже порога даже для крупного текста |
| Почта в футере с `mailto:` | в оригинале схемы не было — ссылка вела в 404 |
| Меню на мобильном — лента с прокруткой | оригинал прятал 3 пункта из 8 |
| Карточки этапов одной высоты | в оригинале лесенка от ручных координат Zero Block |
| Таймлайн «О нас» — прямая ось вместо рисованной змейки | кривые линии были абсолютным декором, не переживающим резину |
| Аккордеон услуг на `<details>`, кейсы на scroll-snap | тот же жест без единого килобайта JS |

Орфография оригинала сохранена дословно («которы», «e-commers», «консъерж» / «консьерж» вперемешку,
«сопровождения клиента») — правки копирайта за заказчиком.

### Проход сверки с оригиналом (2026-08-31, второй заход)

Всё сверено замерами живой страницы, не на глаз:

- **типографика** — заголовки секций 96/900, подзаголовки карточек 400 (а не полужирные), кнопки 24/700;
- **свечения** — точные `box-shadow` оригинала (36 и 72 px, spread 0), радиусы 12/24 px;
- **анимации появления** — те же направления, дистанции (±330 / ±100 / 87 / 164 px) и задержки, что в Тильде;
- **змейка «Все началось в 2013…»** — настоящие шесть кривых Безье из SVG оригинала плюс прорисовка по прокрутке;
- **hero** — колонки 560/580 px и подпись в две строки, как в оригинале.

Метрики после доводки: **Performance 97, Accessibility 100, Best Practices 100, SEO 100**, CLS 0, JS 23 КБ.

**Осталось для показа:** превью-ссылка. Нужен RU-доступный хост (Cloudflare Pages и Vercel не годятся —
режутся из России); решаем вместе с этапом 4.

---

## Этап 2. Форма и лиды

Начинаем после приёмки вёрстки.

- Попап `rec629175892` в разметке есть, но **проверено: его никто не открывает** — ни одного `#popup:`-триггера, все CTA ведут в Telegram. Заказчик прав, формы нет. Беглый взгляд в панель Тильды перед переездом всё же не повредит.
- Форма как в `mate`, **рядом** с кнопкой на бота (кнопка не заменяется).
- Перенос пайплайна из `../mate/mate/src/lib/lead.js` + `workers/lead-relay` (изоморфный код, переносится без правок).
- Telegram: **консьерж-бот `@taotransit_bot`**, который уже стоит на сайте, **отдельная беседа** (новый `CHAT_ID`). Нужен токен бота и добавление его в группу с правом писать. `sendMessage` не конфликтует с вебхуком бота — но `setWebhook` на этом токене не трогать никогда, это сломает живого консьержа.
- amoCRM: тот же аккаунт `mategrouptrade`, **другая воронка** (`AMO_PIPELINE_ID`).
- UTM-метки: `TRACKING_KEYS` как в `mate` — шесть `utm_*` + `gclid`/`yclid`/`fbclid`/`roistat` + `_ym_uid` из куки Метрики.
- Добавить `taotransit.com` в `ALLOWED_ORIGINS` — помнить, что это требует и пуша, и `wrangler deploy`.
- Чекбокс согласия на обработку ПД (152-ФЗ) — `PrivacyModal` из `mate` переиспользуется.
- Сквозная проверка: заявка приходит в нужную беседу TG и в нужную воронку amo, метки заполнены.
- Если форма выйдет раньше нового счётчика (этап 3), поле `_ym_uid` будет пустым — оно читается из куки Метрики. Не поломка, просто ожидаемо.

## Этап 3. Аналитика

- Новый счётчик Яндекс.Метрики (свой).
- Цель на отправку формы, передача `_ym_uid` в лид.
- ⚠️ Домен не меняется, поэтому отказ от текущего счётчика `94685921` = потеря накопленной истории. Решить до отключения.

## Этап 4. Хостинг и переезд домена

- Выбрать площадку (VPS `mate` — кандидат, решаем отдельно; читать § ТСПУ в `mate/CLAUDE.md` до любых действий с nginx/certbot).
- Экспорт полной DNS-зоны: **MX / TXT / SPF / DKIM** — почта на домене чаще всего и ломается при переезде.
- Заранее снизить TTL у A-записи. Решить судьбу DDoS-Guard.
- www→apex редирект.
- **Гейт переключения:** DNS не трогаем, пока форма не подключена и не проверена. Иначе заявки теряются молча, без ошибок на экране.

---

## Решённые вопросы

Все три закрыты 2026-08-31:

1. ~~Какой бот~~ — **закрыт**: `@taotransit_bot`, отдельная беседа. К этапу 2 нужен токен бота от заказчика.
2. ~~Исходники медиа~~ — **закрыт**: то, что на CDN, и есть оригиналы (см. этап 1.1). Осталась мелочь: если у заказчика есть фото складов/кейсов в бо́льшем разрешении вне Тильды — возьмём, но и текущих хватает.
3. ~~Границы улучшений~~ — **закрыт**: адаптив и контраст, композиция без изменений.
